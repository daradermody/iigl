import {Users} from './users';
import {TournamentCodes} from './tournamentCodes';

export namespace storage {
  export const users = Users;
  export const tournamentCodes = TournamentCodes;
}

export default storage;
